{
  "version": 2,
  "builds": [
    { "src": "server/main.py", "use": "@vercel/python" },  
    { "src": "client", "use": "@vercel/react" } 
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/main.py" }, 
    { "src": "/(.*)", "dest": "client" } 
  ]
}
